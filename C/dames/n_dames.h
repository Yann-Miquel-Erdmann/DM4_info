#ifndef n_dames_h
#define n_dames_h

#include <stdio.h>

/* crée la formule pour la problème des n dames su un plateau de n*n cases
   stocke le résultat dans le fichier filename
   renvoie la taille de la chaine générée */
int gen_formule_n_dames(int n, char* filename);

#endif
